# Dependencias del pipeline Agente_YT
# Validacion de datos (esquemas estrictos del JSON de salida)
pydantic>=2.6,<3.0

# Carga de variables de entorno (claves de API) desde .env
python-dotenv>=1.0

# Cliente HTTP (Nodo 5: Higgsfield API / futuras integraciones)
httpx>=0.27

# --- Proveedores LLM (opcionales segun el que uses) ---
# Descomenta el que vayas a usar. El modo "mock" NO requiere ninguno.
# anthropic>=0.40   # proveedor "anthropic" (Claude)
# openai>=1.40      # proveedor "openai" (GPT) Y "nvidia" (comparten SDK)

# --- Nodo 7: montaje (opcional) ---
# Solo si NO tienes ffmpeg en el PATH: trae un binario estatico de ffmpeg.
# imageio-ffmpeg>=0.5

# --- Nodo 8: voz / TTS (opcionales segun proveedor) ---
# El modo "mock" NO requiere ninguno. Descomenta el que uses:
# edge-tts>=6.1     # gratis, sin clave (recomendado)
# gTTS>=2.5         # gratis, sin clave
